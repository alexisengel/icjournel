<!--pages/survey/surveyList.wxml-->
<view class="container">
  <text>Available Surveys</text>
  <scroll-view class="surveys" wx:for="{{surveys}}" id="{{index}}">
    <text bindtap="chooseSurvey" data-survey_id="{{item.id}}">{{item.name}}</text>
  </scroll-view>
</view>
